namespace Countdown.ViewModels
{
    /// <summary>
    /// A list item used in the letters result ui list
    /// </summary>
    internal sealed class WordItem : ItemBase
    {
        public WordItem(string item) : base(item)
        {
        }
    }
}
