<Page
    x:Name="SettingsViewPage"
    NavigationCacheMode="Enabled"
    x:Class="Countdown.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Countdown.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Countdown.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="20, 20, 0, 0">

        <StackPanel>
            <TextBlock Text="Theme Mode" Style="{ThemeResource SubtitleTextBlockStyle}"/>
            <RadioButton Content="Light" IsChecked="{x:Bind vm:SettingsViewModel.IsLightTheme, Mode=TwoWay}"/>
            <RadioButton Content="Dark" IsChecked="{x:Bind vm:SettingsViewModel.IsDarkTheme, Mode=TwoWay}"/>
            <RadioButton Content="Use system setting" IsChecked="{x:Bind vm:SettingsViewModel.IsSystemTheme, Mode=TwoWay}"/>
        </StackPanel>
    </Grid>
</Page>
