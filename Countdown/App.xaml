<Application
    x:Class="Countdown.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="using:Countdown.Views">
    
    <Application.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
                <ResourceDictionary Source="ms-appx:///Microsoft.UI.Xaml/DensityStyles/Compact.xaml" />
                <ResourceDictionary Source="ms-appx:///Resources/Themes.xaml" />
            </ResourceDictionary.MergedDictionaries>
           
            <Style x:Key="StartStopButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
                <!-- stops the layout adjusting when the button's content changes -->
                <Setter Property="MinWidth" Value="105"/>
            </Style>

            <!-- common list style -->
            <Style TargetType="ListView">
                <Setter Property="Margin" Value="5"/>
                <Setter Property="SelectionMode" Value="Extended"/>
                <Setter Property="Background" Value="{ThemeResource ResultsListBackgroundBrush}"/>
                <Setter Property="BorderBrush" Value="{ThemeResource ResultsListBoderBrush}"/>
                <Setter Property="BorderThickness" Value="{ThemeResource ResultsListBoderThickness}"/>
                <Setter Property="CornerRadius" Value="6"/>
            </Style>

            <!-- explicit border style (an implicit style has side effects on Viewboxs, ScrollViewers etc.) -->
            <Style x:Key="CommonBorderStyle" TargetType="Border">
                <Setter Property="Margin" Value="7"/>
                <Setter Property="Padding" Value="7"/>
                <Setter Property="BorderBrush" Value="{ThemeResource ResultsListBoderBrush}"/>
                <Setter Property="BorderThickness" Value="{ThemeResource ResultsListBoderThickness}"/>
                <Setter Property="Background" Value="{ThemeResource ApplicationPageBackgroundThemeBrush}"/>
                <Setter Property="CornerRadius" Value="4"/>
            </Style>

            <!-- entry fields heading text -->
            <Style x:Name="HeadingStyle" TargetType="TextBlock" BasedOn="{StaticResource BodyTextBlockStyle}">
                <Setter Property="Margin" Value="7,0,0,5"/>
            </Style>
            
            <!-- make all the result lists start at the same x coordinate -->
            <GridLength x:Key="InputGridWidth">340</GridLength>

            <!-- window content control with global visual states -->
            <Style x:Key="WindowContentStyle" TargetType="ContentControl">
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="VerticalContentAlignment" Value="Stretch" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ContentControl">
                            <ContentPresenter x:Name="cp" Content="{TemplateBinding Content}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}">

                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="DesktopBackdropStates">
                                        <VisualState x:Name="BackdropNotSupported">
                                            <VisualState.Setters>
                                                <Setter Target="cp.Background" Value="{ThemeResource ApplicationPageBackgroundThemeBrush}"/>
                                            </VisualState.Setters>
                                        </VisualState>

                                        <VisualState x:Name="BackdropValid"/>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </ContentPresenter>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            
        </ResourceDictionary>
    </Application.Resources>
</Application>

