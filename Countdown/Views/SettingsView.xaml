<Page
    x:Name="SettingsViewPage"
    x:Class="Countdown.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Countdown.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    NavigationCacheMode="Required"
    mc:Ignorable="d">

    <StackPanel Margin="50,40,0,0">
        
        <TextBlock Text="App Theme" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <RadioButton Content="Light" IsChecked="{x:Bind ViewModel.IsLightTheme, Mode=TwoWay}"/>
        <RadioButton Content="Dark" IsChecked="{x:Bind ViewModel.IsDarkTheme, Mode=TwoWay}"/>
        <RadioButton Content="Use system setting" IsChecked="{x:Bind ViewModel.IsSystemTheme, Mode=TwoWay}"/>

        <StackPanel Orientation="Horizontal" Margin="0,60,0,0">
            
            <Image x:Name="AboutImage" Width="62" Height="62" Margin="0,7,20,0" VerticalAlignment="Top"/>

            <StackPanel Margin="10,0,0,0">
                <TextBlock Text="Countdown" FontSize="20"/>
                <TextBlock Text="Version: {0}" x:Name="VersionTextBlock"/>
                <TextBlock><Hyperlink NavigateUri="https://github.com/DHancock/Countdown">GitHub Repository</Hyperlink></TextBlock>
            </StackPanel>
        </StackPanel>
    </StackPanel>
</Page>
